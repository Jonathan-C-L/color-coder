// interface MainProps {
//     count: number;
//     setCount: (operation: (value:number) => number) => void;
// }

export const Export = () => {
    return (
        <section id="center" className="container">
            <button
            type="button"
            className="export"
            >
            Export Palette
            </button>
        </section>
    );
};