// interface MainProps {
//     count: number;
//     setCount: (operation: (value:number) => number) => void;
// }

export const Export = () => {
    return (
        <section id="export" className="container">
            <button type="button">
                Export Palette
            </button>
        </section>
    );
};