export const Palette = () => {
    return (
        <section  className="container">
            {/* Palette content will be rendered here */}
            <div id="palette"></div>
        </section>
    );
};