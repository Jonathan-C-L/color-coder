export const ViewPort = () => {
    return (
        <main className="container">
            <canvas id="viewport">
                {/* Viewport content will be rendered here */}
            </canvas>
        </main>
    );
};